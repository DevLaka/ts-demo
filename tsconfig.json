{
  "compilerOptions": {
    "allowJs": true,
    // Allow JS files to be a part of the program.
    // Use the checkJS option to get errors from JS files.
    // TS can be add to an existing JS App.
    // TS is a super set of JS, which means that all JS is valid TS.

    "checkJs": true,
    // Enable error reporting in JavaScript files.

    "outDir": "build",
    "target": "ES6",
    // If only using TS to type checking and rely on another tool,
    // such as the Babel compiler to transpile the final output,
    // esnext is a better option.
    // Esnext tells TS to emit code that of latest version of JS,
    // and pass the rest of the code onto the other tools.

    "noEmit": true
    // If only using TS to type checking, setting the noEmit to true, won't
    // generate any JS files.
  },
  "include": ["src/**/*"]
}
